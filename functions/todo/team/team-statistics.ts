/* import * as functions from 'firebase-functions';
// import { NotificationEngine } from './../shared/_classes/notification-engine';
// import { generateTeamStatistics } from './index';

export const cronTeamStatistics = functions
  .onRun(() => {
    /* try {
        generateTeamStatistics();
        NotificationEngine.sendSlackNotification('Die Mannschaftsstatistik wurde aktualisiert.');
    } catch (e) {
        NotificationEngine.notifyAdminError(e);
        NotificationEngine.sendSlackNotification(e);
        return e;
    }
    return true;
  }); */
